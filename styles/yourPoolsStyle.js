import {StyleSheet} from "react-native";

const yourPoolStyle = StyleSheet.create({
    container: {
      flex: 1,
      justifyContent: 'center',
      backgroundColor: "#fff",
      margin: 30,
      gap: 20, 
    },
    button:{
      
    }
  });
  
export default yourPoolStyle;